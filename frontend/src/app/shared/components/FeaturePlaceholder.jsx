import RoleFeaturePlaceholder from './RoleFeaturePlaceholder';

/** Placeholder fitur yang route-nya sudah ada tetapi UI belum lengkap */
export default function FeaturePlaceholder({ title, description }) {
  return <RoleFeaturePlaceholder title={title} description={description} />;
}
